<Window x:Class="CheckerDSO.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CheckerDSO.Views"
        xmlns:vm="clr-namespace:CheckerDSO.ViewModels"
        xmlns:conv="clr-namespace:CheckerDSO.Converters"
        mc:Ignorable="d"
        Title="DSO Account Checker" Height="600" Width="900"
        Background="{StaticResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <conv:StatusToColorConverter x:Key="StatusColorConverter"/>
        <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Control Panel -->
        <Border Grid.Row="0" Background="{StaticResource PanelBackgroundBrush}" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <WrapPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Load Logs" Command="{Binding LoadLogsCommand}"/>
                    <Button Content="Load Proxies" Command="{Binding LoadProxiesCommand}"/>
                    <Button Content="START" Command="{Binding StartCommand}" Background="#a6e3a1"/>
                    <Button Content="STOP" Command="{Binding StopCommand}" Background="#f38ba8"/>
                    <Button Content="Export Unverified" Command="{Binding ExportUnverifiedCommand}" Background="#f9e2af" Foreground="#181825"/>
                </WrapPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Threads: " Margin="0,0,5,0" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ThreadCount}" Width="20"/>
                    <Slider Minimum="1" Maximum="50" Value="{Binding ThreadCount}" Width="100" Margin="5,0,0,0" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content (DataGrid + Stats) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- DataGrid -->
            <DataGrid Grid.Column="0" ItemsSource="{Binding Accounts}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True" Background="{StaticResource PanelBackgroundBrush}"
                      BorderThickness="0" RowBackground="#1e1e2e" AlternatingRowBackground="#181825"
                      Foreground="{StaticResource TextBrush}" GridLinesVisibility="None" HeadersVisibility="Column"
                      Margin="0,0,10,0">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#11111b"/>
                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Foreground" Value="{Binding Status, Converter={StaticResource StatusColorConverter}}"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                    <DataGridTextColumn Header="Password" Binding="{Binding Password}" Width="1*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="1*"/>
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Stats Panel -->
            <Border Grid.Column="1" Background="{StaticResource PanelBackgroundBrush}" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="STATISTICS" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" HorizontalAlignment="Center"/>
                    
                    <Grid Margin="0,5">
                        <TextBlock Text="Total Loaded:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TotalCount}" HorizontalAlignment="Right" FontWeight="Bold"/>
                    </Grid>
                    <Separator Background="#313244" Margin="0,5"/>
                    
                    <Grid Margin="0,5">
                        <TextBlock Text="Verified (🟢):"/>
                        <TextBlock Text="{Binding VerifiedCount}" HorizontalAlignment="Right" Foreground="#a6e3a1" FontWeight="Bold"/>
                    </Grid>
                    
                    <Grid Margin="0,5">
                        <TextBlock Text="Unverified (🟡):"/>
                        <TextBlock Text="{Binding UnverifiedCount}" HorizontalAlignment="Right" Foreground="#f9e2af" FontWeight="Bold"/>
                    </Grid>
                    
                    <Grid Margin="0,5">
                        <TextBlock Text="Wrong Pass (🔴):"/>
                        <TextBlock Text="{Binding WrongPassCount}" HorizontalAlignment="Right" Foreground="#f38ba8" FontWeight="Bold"/>
                    </Grid>
                    
                    <Grid Margin="0,5">
                        <TextBlock Text="Error (⚪):"/>
                        <TextBlock Text="{Binding ErrorCount}" HorizontalAlignment="Right" Foreground="#bac2de" FontWeight="Bold"/>
                    </Grid>
                    
                    <Grid Margin="0,5">
                        <TextBlock Text="Captcha (🟠):"/>
                        <TextBlock Text="{Binding CaptchaCount}" HorizontalAlignment="Right" Foreground="#fab387" FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource PanelBackgroundBrush}" CornerRadius="4" Padding="10,5" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" FontStyle="Italic"/>
        </Border>
    </Grid>
</Window>
